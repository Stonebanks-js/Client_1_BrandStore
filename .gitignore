node_modules/
.next/
out/
next-env.d.ts
.DS_Store
*.tsbuildinfo
# Secrets: every .env variant is ignored except the checked-in template.
.env
.env.*
!.env.example

# Raw generation output (large PNGs). Only the optimised .webp in public/ ships.
assets/generated/

.playwright-mcp/
